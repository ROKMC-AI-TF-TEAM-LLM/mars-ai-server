# L40 운영 — venv-llm (vLLM 서빙 전용, 포트 8000)
#
# requirements.txt의 "서빙 코어" 블록과 동일하다.
# venv-app과 **같은 환경에 설치할 수 없다**: vllm은 transformers==4.57.1을,
# FlagEmbedding은 transformers==4.44.2를 정확히 못 박아 겹치는 버전이 없다.
# 그래서 pip download도 두 파일을 따로 돌려야 한다 (한 번에 하면 ResolutionImpossible).

vllm==0.11.0
torch==2.8.0
transformers==4.57.1
torchvision==0.23.0
torchaudio==2.8.0
tokenizers==0.22.1
# ⚠️ requirements.txt는 triton==3.5.0으로 적혀 있으나 **설치 불가**다.
# torch 2.8.0이 triton==3.4.0을 정확히 못 박으므로 3.5.0과 공존할 수 없다
# (vllm 0.11.0은 triton을 직접 요구하지 않는다). Windows에는 triton이 없어
# 개발에서 드러나지 않았고, 내부망 설치 시점에 터졌을 값이다
triton==3.4.0

# ── 반입 번들 중복 제거용 고정 (venv-app과 같은 버전) ────────────────────
# 아래는 vllm이 끌고 오는 전이 의존성이다. 고정하지 않으면 최신으로 해석돼
# venv-app과 다른 버전이 되고, **번들에 같은 패키지가 두 개 들어간다.**
# 반입 승인은 파일 단위라 개수가 곧 절차 비용이다.
#
# vllm의 하한을 모두 만족하는 값으로 골랐고(requests>=2.26.0, openai>=1.99.1),
# AlmaLinux 9에서 --no-index 설치와 vllm import를 확인했다.
openai==1.109.1
requests==2.32.3
packaging==24.2
python-dotenv==1.0.1
dill==0.3.8
fsspec==2024.3.1
uvicorn==0.34.0
pydantic-settings==2.7.0

# ⚠️ 아래 7종은 **통일할 수 없다** (전부 실측으로 ResolutionImpossible 확인).
# 번들에 두 버전이 남는 것이 정상이며, 억지로 맞추려 하지 말 것.
#
#   transformers  vllm>=4.55.2      vs FlagEmbedding==4.44.2   ← venv 분리 사유 그 자체
#   tokenizers    transformers 4.57.1이 >=0.22.0을 요구 (venv-app은 0.19.x)
#   numpy         numba가 <2.3을 못 박는다 (venv-app은 2.4.6)
#   pydantic      vllm>=2.11.7      vs venv-app 2.10.4
#   pydantic-core pydantic을 따라간다
#   starlette     vllm이 끌고 오는 prometheus-fastapi-instrumentator가 >=1.0.0을
#                 요구하는데, fastapi 0.115.6은 <0.42.0을 요구한다
#   fastapi       ★ fastapi 자체가 아니라 **starlette에 끌려 올라간 것**이다.
#                 vllm의 요구는 >=0.115.0뿐이라 0.115.6도 만족하지만,
#                 위 starlette 충돌 때문에 새 fastapi(0.141.1)를 쓸 수밖에 없다.
#                 합치려면 venv-app을 0.141.1로 올려야 하고, 그때 pydantic까지
#                 함께 움직여 API 계약(SSE) 재검증이 필요하다 — 별도 과제다

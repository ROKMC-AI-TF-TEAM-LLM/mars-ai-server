# L40 운영 — venv-app (MARS 앱 + 임베딩 8001 + 리랭커 8002)
#
# venv-llm과 분리한다 (사유는 requirements-linux-llm.txt 참조).
# transformers는 여기서 고정하지 않는다 — FlagEmbedding이 4.44.2를 끌고 온다.
#
# ⚠️ setuptools를 맨 앞에 둔다: pymilvus 2.5.4가 pkg_resources를 요구하는데
# setuptools 81+에서 제거됐다. 빠뜨리면 내부망에서 최신 버전이 깔려 터진다
setuptools==75.6.0

# ── LangGraph / LangChain 스택 ──
langgraph==0.2.62
langchain-core==0.3.29
langchain-openai==0.2.14
langchain-text-splitters==0.3.4

# ── 벡터DB (Milvus Lite — 운영은 파일 기반) ──
# 3.x는 저장 포맷이 다르다 (파일 → 디렉터리). 2.4.11로 만든 DB는 읽지 못하므로
# 버전을 올릴 때는 전체 재적재가 필요하다 — docs/milvus_lite_3x.md §2-1
pymilvus==2.5.4
milvus-lite==3.2.1
faiss-cpu==1.15.0

# ── 임베딩 / 리랭커 ──
torch==2.8.0
FlagEmbedding==1.3.3

# ── 하이브리드 검색: BM25 ──
kiwipiepy==0.22.2
bm25s==0.2.5

# ── 문서 파서 (PDF) ──
pdfplumber==0.11.10

# ── API 서버 ──
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4

# ── 유틸 ──
python-dotenv==1.0.1
requests==2.32.3
